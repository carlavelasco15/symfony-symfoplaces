{% extends 'base.html.twig' %}

{% block title %}Place{% endblock %}

{% block main %}
    <h1>Place</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ place.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ place.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ place.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ place.type }}</td>
            </tr>
            <tr>
                <th>Valoration</th>
                <td>{{ place.valoration }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ place.country }}</td>
            </tr>
            <tr>
                <th>Village</th>
                <td>{{ place.village }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('place_list') }}">back to list</a>

    {# <a href="{{ path('place_edit', {'id': place.id}) }}">edit</a> #}

    {# {{ include('place/_delete_form.html.twig') }} #}
{% endblock %}
